spring:
  main:
    allow-bean-definition-overriding: true
  application:
    name: zcloud-health-service
  cloud:
    nacos:
      config:
        server-addr: nacos.zealsoft.com.cn:8848
        file-extension: yaml
  profiles:
    active: dev
feign:
  sentinel:
    enabled: true